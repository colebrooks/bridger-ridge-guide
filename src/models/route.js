class Route {
    constructor(path, desc, camera, pitch, heading) {
        this.path = path;
        this.desc = desc;
        this.camera = camera;
        this.pitch = pitch;
        this.heading = heading;
    }
}

export { Route };